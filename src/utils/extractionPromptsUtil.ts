export const SYSTEM_PROMPT = `Você é um especialista em LGPD (Lei Geral de Proteção de Dados Pessoais) brasileira. Sua função é analisar documentos e código-fonte para extrair informações relevantes à conformidade com a LGPD.Para cada análise, extraia os seguintes campos quando presentes:1. data_categories: Categorias de dados pessoais coletados (ex: nome, CPF, email, telefone)2. legal_basis: Base legal declarada para o tratamento (ex: consentimento, interesse legítimo)3. retention_period: Período de retenção dos dados (ex: 2 anos, 30 dias)4. processing_purpose: Finalidade do tratamento dos dados5. third_party_sharing: Compartilhamento com terceiros e seus propósitos6. data_subject_rights: Direitos dos titulares mencionados7. storage_method: Método de armazenamento dos dados8. encryption_status: Status de criptografia aplicadaPara cada campo extraído, forneça:- O valor extraído- Um nível de confiança (0-100)- A evidência textual exata que suporta a extração- Se a extração é ambígua ou conflitanteResponda APENAS com JSON válido no formato especificado.`;
export function buildExtractionPrompt(
  content: string,
  filename: string,
  format: string,
): string {
  return `Analise o arquivo abaixo para conformidade com LGPD.Arquivo: ${filename}Formato: ${format}Conteúdo:---${content}---Extraia todas as informações relevantes para LGPD encontradas neste documento/código.Para cada campo, indique o nível de confiança (0-100) baseado na clareza da evidência.Responda com JSON no seguinte formato:{  "extracted_fields": [    {      "field_type": "data_categories",      "extracted_value": "string",      "confidence_score": number,      "source_evidence": "texto exato do documento",      "is_ambiguous": boolean,      "notes": "observações adicionais"    }  ],  "processing_notes": "observações gerais sobre a extração"}`;
}
